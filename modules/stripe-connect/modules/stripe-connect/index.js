import React from "react";
// @ts-ignore

import Navigator from "./Navigator";

const App = () => {
  return (
    <Navigator />
  );
};

export default {
  title: "Stripe Connect",
  navigator: App
};
